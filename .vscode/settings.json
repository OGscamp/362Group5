// Workspace settings
{
    // The following will hide the js and map files, as well as the node_modules directory
    "files.exclude": {
        "Server/**/*.js": true,
        "Server/**/*.map": true,
        "**/node_modules/": true
    }
}